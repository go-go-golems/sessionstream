# Changelog

## 2026-04-20

- Created ticket EVT-STREAM-010 for the old Phase 6 migration/regression concept.
- Reframed Phase 6 into a concrete `cmd/web-chat` port and migration-playbook phase whose goal is to sever `pkg/webchat` from the live runtime path rather than maintain an indefinite compatibility layer.
- Rewrote `design-doc/01-phase-6-implementation-plan.md` as a detailed intern-facing analysis, design, and implementation guide for porting `cmd/web-chat` onto `evtstream`.
- Updated the ticket index and task list to match the new cutover-oriented scope.
- Prepared the main Phase 6 design guide for reMarkable upload.
- Added `design-doc/02-web-frontend-migration-and-design-guide.md` as a separate detailed frontend migration/design document covering the current web architecture, UX continuity requirements, integration rewrite strategy, and intern-facing execution guidance.
- Updated `design-doc/01-phase-6-implementation-plan.md` with a dedicated condensed frontend migration strategy section, explicit frontend work checklist, and a pointer to the detailed companion guide.
- Revised the Phase 6 docs to remove the proposed runtime compatibility layer, define a clean-cut canonical web contract strategy instead, and align the frontend guide/tasks with a lockstep frontend+backend migration.
- Added a concrete slice-by-slice implementation guide to `design-doc/01-phase-6-implementation-plan.md` so the ticket can be executed with narrow commits and browser validation as work lands.
- Added `design-doc/03-current-cmd-web-chat-behavior-inventory.md` to capture the current public/runtime behavior that the cutover must consciously preserve, replace, or drop.
- Added `design-doc/04-preserve-change-drop-matrix.md` to classify product behavior versus legacy runtime/wire details.
- Rewrote `tasks.md` into execution slices and started `reference/01-investigation-diary.md` for detailed step-by-step implementation tracking.
- Implemented the first code slice by introducing `pkg/evtstream/apps/chat`, adding an app-facing chat service surface, wrapping the old `examples/chat` package as a thin shim, and switching Systemlab Phase 4 to consume the new app-grade package surface.
- Added `design-doc/05-canonical-web-contract-and-package-layout.md` to freeze the target `cmd/web-chat` app package layout and the canonical submit/snapshot/websocket/profile contract before the next handler/frontend slices.
- Implemented the next backend slice in `cmd/web-chat` by adding app-owned canonical evtstream-backed handlers under `cmd/web-chat/app`, mounting `/api/chat/sessions` + `/api/chat/ws` in the real command, and adding focused handler/websocket tests for the new path.
- Switched the main web frontend to the canonical session-based routes and websocket contract, updated Vite proxying for `/api` websocket traffic, and browser-validated a real send + reload/reconnect flow against a running `cmd/web-chat` server using the `gpt-5-nano-low` profile selection.
- Extended the canonical app path so it can use the real timeline DB/DSN flags for SQLite-backed hydration, added a restart-persistence test for the migrated handler path, and captured `reference/02-migrated-session-snapshot.json` from a real browser-driven migrated session.
- Removed `pkg/webchat` from the default live `cmd/web-chat` runtime wiring, switched the command to serve the canonical `cmd/web-chat/app` routes plus embedded UI directly, added focused handler/root-mount tests, rebuilt the frontend bundle, and captured `reference/03-live-runtime-session-snapshot.json` plus `reference/04-live-route-cutover-checks.json` as cutover evidence.
- Added a deterministic migration comparison regression test in `pinocchio/cmd/web-chat/migration_comparison_test.go`, captured `reference/05-legacy-flow-transcript.json` and `reference/06-canonical-flow-transcript.json`, and summarized the observed preserve/change/drop decisions in `reference/07-legacy-vs-canonical-flow-comparison.md`.
- Wired the canonical `cmd/web-chat/app` submit path to resolved runtimes instead of the echo-only demo backend, translated runtime inference events into canonical evtstream chat events, emitted/persisted user messages as separate canonical chat entities, updated frontend snapshot/live mapping to preserve `role` and `content`, rebuilt the frontend bundle, and captured `reference/08-runtime-wired-user-message-snapshot.json` plus browser evidence showing the user message in the migrated UI.
- Re-ran the canonical app path against the normal pinocchio config/profile stack instead of the earlier ad hoc minimal registry, confirmed a successful real provider-backed `gpt-5-nano-low` run, browser-validated that the migrated UI renders both the user message and real assistant output, and captured `reference/09-real-provider-backed-session-snapshot.json`.
- Built the first Phase 6 Systemlab migration/regression console in `cmd/evtstream-systemlab`, added a dedicated Phase 6 chapter/page/API/test, validated it against the live canonical `cmd/web-chat` server, and captured `reference/10-systemlab-phase-6-run.json` as transcript-backed inspection evidence.
- Added `reference/11-final-cutover-recommendation-and-legacy-removal-plan.md`, formally recommending acceptance of the canonical `evtstream` cutover, documenting why legacy routes should stay removed, and classifying the remaining `pkg/webchat`-era traces as immediate deletion targets, temporary historical harnesses, or deferred tutorial artifacts.
- Deleted `pinocchio/cmd/web-chat/profile_policy.go` after moving its remaining legacy request-resolution adapter logic into `pinocchio/cmd/web-chat/migration_comparison_test.go`, so the live `cmd/web-chat` main package no longer imports `pkg/webchat/http` through that shim.
- Deleted `pinocchio/cmd/web-chat/migration_comparison_test.go`, removed the old `cmd/web-chat/thinkingmode/*` backend/tutorial island plus `cmd/web-chat/web/src/features/thinkingMode/*`, and cleaned the remaining `thinking_mode` / `pkg/webchat` references out of the `cmd/web-chat` tree.
- Deleted the legacy `pinocchio/pkg/webchat` package entirely, removed its remaining live documentation pages from `pinocchio/pkg/doc`, updated the surviving intern tutorial and runtime-symbol migration note to the post-sessionstream architecture, and revalidated the live `cmd/web-chat`, `pkg/chatapp`, and help/doc surfaces (code commit `0d3f639e2d26f0d6972f43cf69d3a42cb2ad79a8`).
